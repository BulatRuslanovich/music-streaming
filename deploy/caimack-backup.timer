[Unit]
Description=Nightly Caimack backup

[Timer]
OnCalendar=*-*-* 04:30:00
# Сервер мог быть выключен в 04:30 — доганяем при следующем старте.
Persistent=true
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
