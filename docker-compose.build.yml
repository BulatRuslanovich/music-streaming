
services:
  backend:
    build:
      context: ./backend
      target: runtime
      args:
        GIT_SHA: ${GIT_SHA:-}

  frontend:
    build:
      context: ./frontend
      args:
        GIT_SHA: ${GIT_SHA:-}
