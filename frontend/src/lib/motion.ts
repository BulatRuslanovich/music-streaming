// Mirrors --ease / --transition from app/styles/tokens.css so JS-driven and CSS-driven
// animation use the same curve.
export const EASE: [number, number, number, number] = [0.2, 0, 0, 1];
export const DURATION = 0.16;
