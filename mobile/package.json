{
  "name": "mobile",
  "version": "1.1.0",
  "private": true,
  "description": "Нативная оболочка Android поверх веб-приложения Caimack",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "run": "cap run android",
    "apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@types/node": "^20.19.43",
    "typescript": "^5"
  }
}
